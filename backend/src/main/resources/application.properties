# Server Port
server.port=8080

# Spring Profiles
spring.profiles.active=dev

# Application Name
spring.application.name=student-attendance-erp-backend

# Default Embedded H2 Database (Enables zero-setup, instant execution out-of-the-box)
spring.datasource.url=jdbc:h2:file:./data/attendance_erp_db;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

# H2 Web Console (Accessible at http://localhost:8080/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.h2.console.settings.web-allow-others=true

# Logging
logging.level.com.erp.attendance=INFO
logging.level.org.hibernate.SQL=INFO
